import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_everything',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
